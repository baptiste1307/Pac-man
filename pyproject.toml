[project]
name = "fly_in"
version = "0.1.0"
description = "packages needed for the fly_in project"
requires-python = ">=3.10"

dependencies = [
    "pydantic",
    "numpy",
    "mypy",
    "flake8",
    "pygame-ce",
]