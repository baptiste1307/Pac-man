[project]
name = "pac-man"
version = "0.1.0"
description = "packages needed for the pac-man project"
requires-python = ">=3.10"

dependencies = [
    "pydantic",
    "numpy",
    "mypy",
    "flake8",
    "pygame-ce",
]